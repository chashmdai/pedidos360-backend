# Real configuration lives outside the repository; see scripts/setup-local.sh.
# The local profile uses a separate loopback JWT fixture, not Microsoft Entra.
# scripts/entra_config.py derives these public values from config/entra-public.json:
# JWT_ISSUER=
# JWT_AUDIENCE=
# JWT_JWKS_URI=
# JWT_TENANT_ID=
# JWT_CLIENT_ID=
